# Контекст проекта Tebe Poveryat

Привет!
Вот состояние проекта на **05.12.2025** (Сессия завершена).

## 🏗 Архитектура
*   **Hybrid/Classic:** `front-page.php` вызывает `template-parts/home/*.php`.
*   **Styles:** Tailwind CSS v4 (в `src/input.css`, сборка в `assets/css/output.css`). `npm run dev`.
*   **JS:** `assets/js/main.js` (меню), `assets/js/sliders.js` (слайдеры).
*   **Libs:** Swiper.js (в `assets/vendor/swiper/`).

## 🚦 Текущий статус: SLIDERS DONE & MOBILE READY
1.  **Главная страница:** Верстка Mobile First готова.
2.  **Слайдеры:** 5 активных слайдеров (Hero, Friends, Media, Materials, Histories, Team). Секция **Projects** — статичная (не слайдер).
3.  **UI Kit:** Компоненты (`button`, `link-more`...) внедрены везде.
4.  **Баги:** Критическая ошибка в `functions.php` исправлена.

## 📝 План действий (Next Steps)
1.  **Адаптив (Desktop):** Главная задача сейчас. Превратить мобильную верстку в десктопную.
    *   Использовать `md:grid-cols-2`, `lg:grid-cols-3` и т.д.
2.  **Функционал:**
    *   Доделать мобильное меню (анимации).
    *   PHP-обработчики форм.
3.  **Страницы:** `single.php` и другие.

## ⚠️ Важно
*   Всегда запускай `npm run dev`.
*   Слайдеры инициализируются в `assets/js/sliders.js`. Если меняешь HTML структуру слайдера — правь JS селекторы.
