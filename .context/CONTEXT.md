# Контекст проекта Tebe Poveryat

Привет!
Вот состояние проекта на **05.12.2025**.

## 🏗 Архитектура
*   **Hybrid/Classic:** `front-page.php` вызывает `template-parts/home/*.php`.
*   **Styles:** Tailwind CSS v4 (в `src/input.css`, сборка в `assets/css/output.css`).
*   **Fonts:** Geologica (Google), Akrobat & Ura Bum Bum SP (Local).
*   **Components:** UI Kit в `template-parts/components/`.

## 🚦 Текущий статус: UI KIT, MOBILE DRAFT & SLIDERS ACTIVE
1.  **Главная страница:** Сверстана (Mobile layout) с использованием компонентов.
2.  **UI Kit:** Выделены основные компоненты и применены.
3.  **Слайдеры:** Интегрирован **Swiper.js** во все необходимые секции (Hero, Friends, Media, Materials, Histories, Team). Каждый слайдер настроен на `loop`, `autoplay` и использует наш кастомный `slider-progress` компонент.

## 📝 План действий (Next Steps)
1.  **Адаптив (Desktop):**
    *   Пройтись по секциям и добавить `md:`, `lg:` классы.
2.  **Формы (PHP):**
    *   Реализовать обработчики для форм донатов и подписки.
3.  **Другие страницы:**
    *   Сверстать `single.php`, `page.php`, `404.php`.

## ⚠️ Важно
*   Всегда запускай `npm run dev` при правке классов Tailwind.
*   Все цвета настроены в `input.css` (`@theme`).