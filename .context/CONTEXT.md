# Контекст проекта Tebe Poveryat

Привет, будущий разработчик! (или я сам через время).
Вот состояние проекта на **05.12.2025**.

## 🏗 Архитектура и Стек
Мы используем **Гибридный подход** с уклоном в **Classic PHP + Tailwind CSS**.

*   **CMS:** WordPress.
*   **Тема:** `tebe-poveryat` (Custom Theme).
*   **Стили:** **Tailwind CSS v4** (alpha/beta).
    *   Конфиг: `src/input.css` (используем директиву `@theme`).
    *   Сборка: `npm run dev` (watch) или `./build.sh`.
    *   Файл сборки: `assets/css/output.css`.
*   **Шаблоны:** Классические PHP файлы (`header.php`, `footer.php`, `front-page.php`).
    *   **Компоненты:** Лежат в `template-parts/home/` (все секции главной).
    *   **FSE (Full Site Editing):** Отключен (папка `templates` переименована в `templates_backup`), но `theme.json` используется для настроек.
*   **Шрифты:**
    *   `Geologica` (Google Fonts).
    *   `Akrobat` (Local `.woff2` in `assets/fonts/`).
    *   `Ura Bum Bum SP` (Local `.otf` in `assets/fonts/`).
*   **JS:** Ванильный JS в `assets/js/main.js` (подключен в футере).

## 🎨 Дизайн-система (UI Kit)
Мы верстали Mobile First.
*   **Цвета:** Определены в `input.css` (`--color-primary`, `--color-secondary`, `--color-teal`, `--color-peach` и др.).
*   **Типографика:**
    *   Заголовки секций: `font-ura text-[32px] uppercase`.
    *   Основной текст: `font-geologica font-light`.
    *   Акценты/Кнопки: `font-akrobat font-extrabold`.

## 🚀 Текущий статус
1.  **Главная страница:** Полностью сверстана (Mobile layout). Все секции разбиты на `template-parts/home/*.php`.
2.  **Хедер/Футер:** Готовы (Mobile).
3.  **Мобильное меню:** Готово (HTML + базовый JS открытия).

## 📝 Что нужно сделать (Next Steps)
1.  **UI Kit Refactoring:** Выделить кнопки и ссылки в `template-parts/components/` (см. TODO).
2.  **Адаптив (Desktop):** Пройтись по всем файлам в `template-parts/home/` и добавить классы `md:...`, `lg:...` для десктопа.
3.  **Слайдеры:** Подключить **Swiper.js** через npm (`npm install swiper`) и оживить секции Hero, Friends, Media, Histories, Team.
4.  **Single Post:** Сверстать шаблон отдельной записи.

## ⚠️ Нюансы
*   Если видишь в коде `<!-- wp:group -->` — это остатки старых экспериментов с блоками, в новых файлах `template-parts` мы пишем чистый HTML.
*   SVG часто инлайновые. Если они слишком большие — выноси их в отдельные файлы или спрайты.
*   **Не забывай запускать `npm run dev`** перед началом работы!

Удачи!
