# Контекст проекта Tebe Poveryat

Привет!
Вот состояние проекта на **05.12.2025**.

## 🏗 Архитектура
*   **Hybrid/Classic:** `front-page.php` вызывает `template-parts/home/*.php`.
*   **Styles:** Tailwind CSS v4 (в `src/input.css`, сборка в `assets/css/output.css`).
*   **Fonts:** Geologica (Google), Akrobat & Ura Bum Bum SP (Local).

## 🚦 Текущий статус: ПРОТОТИПЫ
Мы "накидали" верстку главной страницы (Mobile First).
**Это НЕ финальная верстка.**
*   Многие SVG упрощены.
*   Слайдеры — это статичные блоки (нет JS).
*   Отступы и шрифты — "на глаз" по макету, требуют полировки.
*   Адаптива под Desktop **нет**.

## 📝 План действий (Next Steps)
1.  **UI Kit Refactoring:**
    *   Пройтись по `template-parts/home/` и выдернуть повторяющиеся куски (кнопки, заголовки) в отдельные компоненты (`template-parts/components/`).
    *   Это уменьшит дублирование кода перед тем, как мы начнем делать Адаптив.
2.  **Адаптив (Desktop):**
    *   Добавлять `md:`, `lg:` классы в компоненты.
3.  **Интерактивность (JS):**
    *   Подключить `Swiper.js`.
    *   Написать скрипты инициализации для каждого слайдера.
4.  **Polish (Полировка):**
    *   Проверить все SVG.
    *   Допилить Pixel Perfect.

## ⚠️ Важно
*   Используй `npm run dev` при работе.
*   Не забывай коммитить (`git commit`) после каждого крупного этапа.