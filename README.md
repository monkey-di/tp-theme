# Tebe Poveryat - Тема WordPress

Кастомная тема для НКО "Тебе поверят". 
Стек: WordPress (Classic), Tailwind CSS v4 (Alpha), Swiper.js.

## 🐳 Запуск через Docker

Самый быстрый способ поднять проект локально.

1. **Запуск:**
   ```bash
   docker-compose up -d
   ```

2. **Доступы:**
   - Сайт: http://localhost:8080
   - База: `mysql:5.7` (порт 3306)
   - WordPress Admin: (потребуется стандартная установка при первом входе)

3. **Работа с файлами:**
   Папка темы примонтирована в контейнер. Любые изменения в файлах сразу видны на локальном сайте (кроме CSS, см. ниже).

4. Любой коммит + пуш в ветку мастер автоматически загружает все файлы темы на сервер.

---

## 💻 Локальная разработка (CSS)

Стили написаны на Tailwind v4 и требуют компиляции. **Без этого изменения в дизайне не применятся.**

1. **Установка зависимостей:**
   ```bash
   cd wp-content/themes/tebe-poveryat
   npm install
   ```

2. **Режим разработки (Watch):**
   ```bash
   npm run dev
   ```
   *Держать запущенным в терминале. Следит за `src/input.css` и обновляет `assets/css/output.css`.*

3. **Сборка для продакшена:**
   ```bash
   npm run build
   ```
   *Минифицирует CSS и копирует скрипты Swiper.*

---

## 🚀 Деплой

Деплой настроен через `rsync` на сервер `tebe-poveryat.realeasystudio.site`.

### Вариант 1: Автоматически (GitHub Actions)
Любой пуш в ветку `master` запускает деплой.

---

## 📂 Структура и Ассеты

- **Изображения:**
  - Локальные ассеты лежат в `assets/images/`.
  - Динамические изображения (слайдеры) загружаются через админку WP (Custom Post Type `main_slide`).
- **Стили:**
  - `src/input.css` — Исходник (править тут).
  - `assets/css/output.css` — Результат (не трогать).

- **Шаблоны:**
  - `front-page.php` — Главная страница (собирает секции).
  - `template-parts/home/` — Секции главной (Hero, About, Donation и т.д.).
  - `template-parts/components/` — UI элементы (кнопки, стрелки).

---

## ⚠️ Важно
- **Tailwind v4:** Конфиг находится прямо в CSS файле (`@theme`).
- **Мультиязычность:** Реализована примитивно через проверку `tp_is_english()`.
