# Tebe Poveryat (Тема)

**Tebe Poveryat** — это современная блочная тема WordPress (Full Site Editing - FSE), разработанная с упором на гибкость и производительность. Она использует `theme.json` для глобальной стилизации и конфигурации, сводя к минимуму необходимость в сложных PHP-шаблонах или тяжелых CSS-файлах.

## 🚀 Начало работы

Этот проект использует среду на базе Docker для удобства разработки.

### Требования

*   Docker
*   Docker Compose

### Установка и запуск

1.  **Запустите окружение:**
    Выполните следующую команду в корне проекта (там, где находится файл `docker-compose.yml`):
    ```bash
    docker compose up -d
    ```

2.  **Доступ к сайту:**
    *   **Фронтенд:** [http://localhost:8080](http://localhost:8080)
    *   **Админ-панель:** [http://localhost:8080/wp-admin](http://localhost:8080/wp-admin)
        *   *Примечание: При первом запуске вам может потребоваться пройти стандартный мастер установки WordPress.*

3.  **Доступ к базе данных:**
    *   Хост: `db` (внутри контейнеров) / `localhost:3306` (снаружи, если порт проброшен)
    *   Имя базы данных: `wp_database`
    *   Пользователь: `wp_user`
    *   Пароль: `wp_password`

## 📂 Структура темы

Эта тема следует стандартной структуре блочных тем:

```text
tebe-poveryat/
├── assets/         # Статические ресурсы (изображения, шрифты и т.д.)
├── parts/          # Части шаблонов (Хедер, Футер, Сайдбар)
├── patterns/       # Паттерны блоков (готовые макеты блоков)
├── templates/      # Шаблоны страниц (Главная, Одиночная запись, Архив, 404)
├── functions.php   # Настройка темы и подключение скриптов/стилей
├── index.php       # Пустой файл-заглушка (требуется ядром WP)
├── style.css       # Метаданные темы и вспомогательный CSS
└── theme.json      # Глобальная конфигурация, стили и настройки блоков
```

## 🛠 Руководство по разработке

Поскольку это FSE-тема, процесс разработки отличается от классических PHP-тем.

### 1. Глобальные стили и конфигурация (`theme.json`)
Файл `theme.json` является центром визуального оформления темы. Используйте его для настройки:
*   **Цветовая палитра:** Определение основных, вторичных и базовых цветов.
*   **Типографика:** Настройка семейств шрифтов, размеров и правил адаптивной типографики.
*   **Макет:** Определение ширины контента (сейчас установлено `1408px`).
*   **Стили блоков:** Применение стилей по умолчанию к конкретным блокам (например, `core/button`, `core/navigation`).

**Пример: Изменение основного цвета**
Перейдите в `settings.color.palette` в файле `theme.json` и измените запись с названием "Primary".

### 2. Шаблоны и части (Templates & Parts)
*   **Редактирование макетов:** Редактируйте HTML-файлы в папках `templates/` и `parts/`. Эти файлы содержат **разметку блоков** (HTML с комментариями WP), а не PHP.
*   **Новые шаблоны:** Чтобы создать новый пользовательский шаблон, добавьте HTML-файл в `templates/` и зарегистрируйте его в `theme.json` в разделе `customTemplates`.

### 3. Пользовательский CSS
Хотя `theme.json` обрабатывает большую часть стилизации, вы можете добавить свой CSS в файл `style.css`. Этот файл автоматически подключается через `functions.php`.
*   *Лучшая практика:* Используйте `style.css` только для тех стилей, которые невозможно реализовать через `theme.json` или редактор блоков.

### 4. PHP Логика
Используйте `functions.php` для:
*   Подключения сторонних скриптов и стилей.
*   Регистрации пользовательских типов записей (CPT) или таксономий.
*   Добавления кастомных паттернов блоков или фильтров.
*   *Текущее состояние:* Сейчас файл только подключает основной файл стилей.

## 🎨 Советы по настройке

*   **Градиенты и Дуотоны:** Определены в `theme.json` в разделе `settings.color`.
*   **Части шаблона:** Тема на данный момент регистрирует части `header` (шапка) и `footer` (подвал).
*   **Шрифты:** По умолчанию используются системные шрифты для максимальной производительности. Добавить свои шрифты можно в `theme.json` > `settings.typography.fontFamilies`.

## 📦 Контроль версий
*   **Игнорируемые файлы:** Убедитесь, что `node_modules`, `vendor` и локальные конфиги добавлены в `.gitignore`, если вы расширите стек технологий.
*   **Сборка:** В настоящее время шаг сборки (Webpack/Vite) не требуется. Тема использует нативные возможности WordPress.

---
*Версия темы: 100.0 | Требования PHP: 7.4+*